{
  "name": "yambot",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prepare": "factory install-rules",
    "checks": "bun run --cwd packages/checks check",
    "checks:structure": "bun run --cwd packages/checks check:structure",
    "typecheck": "bun run --filter '*' typecheck",
    "test": "bun test",
    "start": "bun run --cwd packages/bot start",
    "dev": "bun run --cwd packages/bot dev"
  },
  "devDependencies": {
    "factory": "file:///C:/Users/brice/dev/blueprint"
  }
}
